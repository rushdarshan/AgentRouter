package dev.darshan.agentrouter.tools;

/**
 * Enum grouping tools by domain capability.
 * Each tool declares one capability for intent-to-tool routing.
 */
public enum ToolCapability {
    WEATHER,
    CRM_QUERY,
    CALCULATOR
}
